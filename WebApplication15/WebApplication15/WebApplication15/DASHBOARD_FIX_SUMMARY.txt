# ?? FIX DASHBOARD - TÓM T?T NHANH

## ? L?I
```
'System.DateTime' does not contain a definition for 'HasValue'
Line 82 in Index.cshtml
```

## ? FIX

### T?:
```csharp
@if (sp.HanSuDung.HasValue)
```

### Thành:
```csharp
object hanSuDungObj = sp.HanSuDung;
DateTime? hanSuDung = hanSuDungObj as DateTime?;

@if (hanSuDung.HasValue)
```

## ?? L? DO
- `ViewBag` là `dynamic` type
- Dynamic type + `.HasValue` = Runtime binding error
- Solution: Cast explicit sang `DateTime?`

## ?? K?T QU?
? Build: Success  
? Compile: No errors  
? Runtime: Fixed  
? Dashboard: Ready

---

**File:** `Areas/Admin/Views/Dashboard/Index.cshtml`  
**Sections:** 2 (C?nh báo h?n s? d?ng + C?nh báo s?p h?t h?n)  
**Status:** ? FIXED
