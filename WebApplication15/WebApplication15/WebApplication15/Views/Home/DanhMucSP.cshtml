@model IEnumerable<WebApplication15.Models.DanhMuc>

<h2>DANH MỤC SẢN PHẨM</h2>
<ul>
    @foreach (var item in Model)
    {
        <li>
            <a href="@Url.Action("Index", "Home", new { id = item.MaDM })">@item.TenDM</a>
        </li>
    }
</ul>

