@*@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    var mapsKey = ViewBag.GoogleMapsKey as string;
}

<div class="container mt-4">
    <h3>Bản đồ cửa hàng</h3>
    <div id="map" style="width:100%; height:450px; border-radius:8px;"></div>
</div>

<script>
  function initMap() {
    // tọa độ mặc định (ví dụ: HCM)
    var center = { lat: 10.800, lng: 106.681 };

    var map = new google.maps.Map(document.getElementById('map'), {
      center: center,
      zoom: 14
    });

    // marker ví dụ
    var marker = new google.maps.Marker({
      position: center,
      map: map,
      title: 'Cửa hàng của bạn'
    });
  }
</script>

<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=@mapsKey&callback=initMap">
</script>*@
