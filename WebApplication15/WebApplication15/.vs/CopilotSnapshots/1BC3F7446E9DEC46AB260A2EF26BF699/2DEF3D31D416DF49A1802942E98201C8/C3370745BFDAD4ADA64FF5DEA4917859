@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập - SkinFood</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <link href="~/CSS/SaoRoi.css" rel="stylesheet" />
    <style>
        .alert-message {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
            min-width: 300px;
            animation: slideIn 0.3s ease;
        }

        @@keyframes slideIn {
            from {
                transform: translateX(400px);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .login-card {
            background: rgba(255, 255, 255, 0.15) !important;
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-radius: 20px !important;
            border: 1px solid rgba(255, 255, 255, 0.25) !important;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.35) !important;
            padding: 2.5rem !important;
            width: 420px;
            max-width: 95vw;
        }

        .login-title {
            font-size: 32px;
            font-weight: 800;
            color: #ffffff !important;
            text-shadow: 0 2px 6px rgba(0,0,0,0.4);
            margin-bottom: 0.5rem;
        }

        .login-subtitle {
            color: rgba(255, 255, 255, 0.85);
            font-size: 14px;
            margin-bottom: 2rem;
        }

        .form-label {
            color: #ffffff !important;
            font-weight: 600;
            font-size: 14px;
            margin-bottom: 0.5rem;
        }

        .login-input {
            height: 50px !important;
            border-radius: 12px !important;
            border: 1px solid rgba(255,255,255,0.3) !important;
            background: rgba(255,255,255,0.2) !important;
            color: white !important;
            font-size: 15px;
            padding-left: 3rem !important;
            transition: all 0.3s ease;
        }

        .login-input::placeholder {
            color: rgba(255, 255, 255, 0.7) !important;
        }

        .login-input:focus {
            border-color: #3b8dff !important;
            box-shadow: 0 0 0 0.25rem rgba(59, 141, 255, 0.25) !important;
            background: rgba(255,255,255,0.25) !important;
        }

        .input-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.2rem;
            z-index: 10;
        }

        .btn-login {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
            border: none !important;
            height: 50px;
            font-size: 16px;
            font-weight: 700;
            border-radius: 12px !important;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .divider {
            display: flex;
            align-items: center;
            text-align: center;
            margin: 1.5rem 0;
        }

        .divider::before,
        .divider::after {
            content: '';
            flex: 1;
            border-bottom: 1px solid rgba(255, 255, 255, 0.3);
        }

        .divider span {
            padding: 0 1rem;
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
        }

        .register-link {
            color: #ffffff !important;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s;
            display: inline-block;
        }

        .register-link:hover {
            color: #667eea !important;
            transform: translateX(3px);
        }

        .home-link {
            position: absolute;
            top: 20px;
            left: 20px;
            z-index: 1000;
        }

        .btn-home {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 25px;
            transition: all 0.3s;
            font-weight: 600;
        }

        .btn-home:hover {
            background: rgba(255, 255, 255, 0.25);
            color: white;
            transform: translateY(-2px);
        }

        @@media (max-width: 576px) {
            .login-card {
                padding: 1.5rem !important;
            }
            .login-title {
                font-size: 26px;
            }
        }
    </style>
</head>
<body>

    <div class="night">
        @for (int i = 0; i < 100; i++)
        {
            <div class="shooting_star"></div>
        }
    </div>

    <div class="home-link">
        <a href="@Url.Action("Index", "Home")" class="btn btn-home">
            <i class="bi bi-house-fill"></i> Trang chủ
        </a>
    </div>

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger alert-dismissible fade show alert-message" role="alert">
            <i class="bi bi-exclamation-circle-fill"></i>
            <strong>Lỗi!</strong> @ViewBag.Error
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show alert-message" role="alert">
            <i class="bi bi-check-circle-fill"></i>
            <strong>Thành công!</strong> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="login-wrapper">
        @using (Html.BeginForm("LoginSubmit", "User", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate" }))
        {
            @Html.AntiForgeryToken()

            <div class="card login-card">
                <h1 class="login-title text-center">
                    <i class="bi bi-box-arrow-in-right"></i> Đăng nhập
                </h1>
                <p class="login-subtitle text-center">Chào mừng bạn trở lại với SkinFood</p>

                <div class="mb-3 position-relative">
                    <label for="Email" class="form-label">
                        <i class="bi bi-envelope-fill"></i> Email
                    </label>
                    <div class="position-relative">
                        <i class="bi bi-envelope-fill input-icon"></i>
                        <input type="email" 
                               id="Email"
                               name="Email" 
                               class="form-control login-input" 
                               placeholder="email@example.com"
                               required 
                               autocomplete="email"
                               autofocus />
                    </div>
                </div>

                <div class="mb-3 position-relative">
                    <label for="MatKhau" class="form-label">
                        <i class="bi bi-lock-fill"></i> Mật khẩu
                    </label>
                    <div class="position-relative">
                        <i class="bi bi-lock-fill input-icon"></i>
                        <input type="password" 
                               id="MatKhau"
                               name="MatKhau" 
                               class="form-control login-input" 
                               placeholder="Nhập mật khẩu"
                               required 
                               autocomplete="current-password"
                               minlength="6" />
                    </div>
                </div>

                <button type="submit" class="btn btn-login w-100 mb-3">
                    <i class="bi bi-box-arrow-in-right"></i> ĐĂNG NHẬP
                </button>

                <div class="divider">
                    <span>hoặc</span>
                </div>

                <p class="text-center mb-0">
                    <span style="color: rgba(255, 255, 255, 0.85);">Chưa có tài khoản?</span>
                    <a href="@Url.Action("Register", "User")" class="register-link">
                        Đăng ký ngay <i class="bi bi-arrow-right"></i>
                    </a>
                </p>
            </div>
        }
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto dismiss alerts after 5 seconds
        setTimeout(function() {
            var alerts = document.querySelectorAll('.alert-message');
            alerts.forEach(function(alert) {
                var bsAlert = new bootstrap.Alert(alert);
                bsAlert.close();
            });
        }, 5000);

        // Form validation
        (function() {
            'use strict';
            var forms = document.querySelectorAll('.needs-validation');
            Array.prototype.slice.call(forms).forEach(function(form) {
                form.addEventListener('submit', function(event) {
                    if (!form.checkValidity()) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        })();
    </script>

</body>
</html>
